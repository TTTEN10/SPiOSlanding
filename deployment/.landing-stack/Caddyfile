{
	admin off
}

# TLS + reverse proxy to the single Express app (API + static Vite build).
safepsy.com, www.safepsy.com {
	encode gzip
	reverse_proxy app:3001
}

# Direct IP access (HTTP) for ops; no TLS on bare IP.
http://51.159.160.246 {
	encode gzip
	reverse_proxy app:3001
}
