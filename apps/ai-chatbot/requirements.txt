# FastAPI and server dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0

# vLLM for GPU inference (installed during Docker build for production)
# Note: vLLM requires CUDA and specific PyTorch versions
# vLLM will be installed during Docker build, with fallback to runtime install
# vllm>=0.6.4 — Mistral-class models (e.g. Dolphin-Mistral-24B) also need mistral_common>=1.5.2 (pulled with vLLM)
# vllm>=0.6.0

# CPU inference (when USE_GPU=false and USE_MOCK_RESPONSES=false).
# torch is installed in Dockerfile from PyTorch CPU wheels (avoids multi-GB CUDA deps + disk exhaustion).
transformers>=4.40.0
accelerate>=0.27.0
sentencepiece>=0.1.99
einops>=0.7.0

# Utilities
python-dotenv>=1.0.1
httpx>=0.27.0
redis>=5.0.0

